doctype html
html
  head
    meta(charset="utf-8")
    meta(name="format-detection" content="telephone=no")
    meta(name="viewport" content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width, height=device-height")
    title Ricebook
    link(rel="stylesheet" href="index.css")
    
    script(type='text/javascript').
        var buildVersion = '#{buildVersion}'
    if devMode
        script(type='text/javascript').
          var livereload = document.createElement('script');
          livereload.src = '//'+ location.host.substr(0,location.host.indexOf(':')) + ':35729/livereload.js';
          document.head.appendChild(livereload);
        - for (var i=0; i < jsLibs.length; i++){
        script(src=jsLibs[i]) 
        - }
        script.
            angular.module('trident.templates',[]);
        - for (var i=0; i < jsfiles.length; i++){
        script(src=jsfiles[i]) 
        - }
    else
        script(src="app.js")     
  body
    navigation
    sidebar
    div.view-container(ui-view="main")



